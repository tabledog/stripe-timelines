// @todo/next Test customers with sources.length > 10.
// - `stripe.customers.listSources` can list sources, per (customer, source_type).
// - @see https://stripe.com/docs/api/customer_bank_accounts/list

// Also add card, bank (test these get inserted)
// - alipay, bitcoin (test these panic).